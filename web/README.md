# AuthEndpoints playground UI

Nuxt 4 + Nuxt UI app that exercises the Demo API.

```bash
pnpm install
pnpm dev
```

Set `NUXT_PUBLIC_API_BASE` (default `http://localhost:5041`). See the root [README](../README.md).
